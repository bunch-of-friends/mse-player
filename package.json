{
  "name": "mse-player",
  "version": "0.0.1-alpha",
  "license": "MIT",
  "contributors": [
    {
      "name": "David Bohunek",
      "email": "bohunek@gmail.com"
    }
  ],
  "scripts": {
    "build": "npm run cleanup && lerna run compile",
    "build:watch": "npm run cleanup && lerna run compile:watch --stream",
    "start:demo": "(cd packages/demo && npm run start)",
    "cleanup": "lerna run cleanup --stream --parallel",
    "demo": "npm run build && (cd packages/demo && npm run start)",
    "lint": "tslint '**/*.ts{,x}' -e '**/**/*d.ts' -e '**/node_modules/**'",
    "reset": "npm run cleanup && lerna clean && npm i",
    "publish": "lerna publish --force-publish=*",
    "postinstall": "lerna bootstrap",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "lerna": "^2.9.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "tslint-eslint-rules": "^5.0.0",
    "typescript": "^2.7.2"
  }
}
