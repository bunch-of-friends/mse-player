{
    "name": "@mse-player/main",
    "version": "0.0.1-alpha1",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "test": "jest --config ../../jest.config.json --rootDir",
        "compile": "tsc",
        "compile:watch": "tsc -w",
        "cleanup": "rimraf -rf dist coverage"
    },
    "dependencies": {
        "@mse-player/core": "^0.0.1-alpha1",
        "@bunch-of-friends/observable": "^1.0.0"
    },
    "publishConfig": {
        "access": "public"
    }
}
